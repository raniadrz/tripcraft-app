server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: send all unknown paths to index.html (React Router handles them)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
